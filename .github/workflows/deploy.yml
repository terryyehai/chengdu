# ============================================
# 🚀 GitHub Pages 自動部署
# ============================================
# 當程式碼推送到 main 分支時，自動部署到 GitHub Pages。
# 純靜態 HTML，不需要任何建置步驟。
# 部署後網址：https://terryyehai.github.io/chengdu/
# ============================================

name: 🚀 Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:  # 允許手動觸發

# 授予 GitHub Pages 部署所需的權限
permissions:
  contents: read
  pages: write
  id-token: write

# 同一時間只允許一個部署
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: 📥 拉取程式碼
        uses: actions/checkout@v4

      - name: 📦 設定 GitHub Pages
        uses: actions/configure-pages@v5

      - name: 📤 上傳靜態檔案
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'  # 整個根目錄就是靜態網站

      - name: 🚀 部署到 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
